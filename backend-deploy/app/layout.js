export const metadata = { title: "Snack Swap Adventure — Leaderboard", description: "Leaderboard & player stats" };
export default function RootLayout({ children }) {
  return (<html lang="en"><body style={{margin:0,fontFamily:"system-ui,sans-serif",background:"#1a1028",color:"#fff"}}>{children}</body></html>);
}
